import { FilterparentPipe } from './filterparent.pipe';

describe('FilterparentPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterparentPipe();
    expect(pipe).toBeTruthy();
  });
});
